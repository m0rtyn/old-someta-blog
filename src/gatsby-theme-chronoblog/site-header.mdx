<!-- @mdx mdx -->
import { jsx } from 'theme-ui'
import styles from './site-header.module.css';
import SometaLogo from 'components/common/SometaLogo';

<MenuMain className={styles.siteHeader}>
  <MenuBlock>
    <Link
      to="/"
      title="So Meta — Такая Мета"
      sx={{
        width: '64px',
        mr: '32px',
        color: 'text',
        display: 'flex',
        alignItems: 'center'
      }}
    >
      <SometaLogo />
    </Link>
  </MenuBlock>

  <MenuBlock>
    <Link to="/about">О проекте</Link>
  </MenuBlock>

  <MenuBlock>
    <Link to="/martyn">Об авторе</Link>
  </MenuBlock>

  <MenuBlock css={{marginLeft: "auto"}}>
    <Link to="/donate">Toss a coin ♪</Link>
  </MenuBlock>
</MenuMain>
