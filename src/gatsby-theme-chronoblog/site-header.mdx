<!-- @mdx mdx -->
import { jsx } from 'theme-ui'
import SometaLogo from 'components/common/SometaLogo';

<MenuMain>
  <MenuBlock>
    <Link
      to="/"
      title="So Meta — Такая Мета"
      sx={{
        width: [40, 64],
        mr: [3, 4],
        display: 'flex',
        alignItems: 'center'
      }}
    >
      <SometaLogo logoColor="light"/>
    </Link>
  </MenuBlock>

  <MenuBlock>
    <Link to="/about">О проекте</Link>
  </MenuBlock>

  <MenuBlock sx={{marginRight: 0, marginLeft: "auto", overflow: "hidden"}}>
    <Link
      to="/donate"
      sx={{
        textOverflow: 'ellipsis',
        overflow: 'hidden',
        whiteSpace: 'nowrap'
      }}
    >
      Toss a coin ♪
    </Link>
  </MenuBlock>
</MenuMain>
