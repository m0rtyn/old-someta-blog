<!-- @mdx mdx -->
import { jsx } from 'theme-ui'
import SometaLogo from 'components/common/SometaLogo';
import { Link as LinkUI } from '@theme-ui/components'


<MenuMain>
  <MenuBlock id="HeaderLogo" /* for control of disapearing main page logo */>
    <Link
      to="/"
      title="So Meta — Такая Мета"
      sx={{
        width: [40, 64],
        mr: [3, 5],
        display: 'flex',
        alignItems: 'center'
      }}
    >
      <SometaLogo logoColor="light"/>
    </Link>
  </MenuBlock>

  <MenuBlock>
    <Link to="/about" sx={{flexShrink: 0}}>О проекте</Link>
  </MenuBlock>

  <MenuBlock>
    <LinkUI href="https://t.me/sometachat" sx={{textDecoration: "none"}}>
      <Flex alignItems="flex-start">
        Чат
        {' '}
        <FontAwesomeIcon icon={['fab', 'telegram-plane']} size="xs"/>
      </Flex>
    </LinkUI>
  </MenuBlock>

  <MenuBlock sx={{marginRight: 0, marginLeft: "auto", overflow: "hidden", color: 'background'}}>
    <Link
      to="/donate"
      sx={{
        display: ["none", "flex"],
        textOverflow: 'ellipsis',
        overflow: 'hidden',
        whiteSpace: 'nowrap',
      }}
    >
      Toss a coin ♪
      
    </Link>
  </MenuBlock>

  <MenuBlock>
    <SocialLinks sx={{ marginLeft: ["auto", 0], color: 'background' }} />
  </MenuBlock>

</MenuMain>
